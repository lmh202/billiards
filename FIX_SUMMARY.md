# Phase 1 训练问题修复总结

## ✅ 问题已修复

经过诊断,发现400个迭代后模型完全学不会进球的**根本原因**:

### 🔴 致命错误: 角度惩罚过度严厉

**原来的代码**:
```python
if cos_theta > 0.95:  # 18度以内
    angle_reward = +1~+2
else:
    # ❌ 只要角度不完美,就给-5到-20的惩罚!
    angle_penalty = min(20.0, np.exp(np.degrees(angle_diff) / 15.0) - 1.0)
    reward -= angle_penalty
```

**问题**:
- 90%以上的随机动作都会触发角度惩罚
- 负反馈(-20)远大于正反馈(+2)
- 模型学到的是"什么都不做"而不是"尝试进球"

**修复后**:
```python
if cos_theta > 0.95:
    angle_reward = +1~+2
elif cos_theta > 0.85:
    reward += 0.5
# ✅ 角度不好就不给奖励,但也不惩罚!
```

---

## 📊 修复效果验证

### 测试结果 (1000步随机动作)

```
奖励均值: +0.03  ✅ (之前是 -15)
奖励标准差: 0.77
奖励范围: -3.51 到 +3.96  ✅
中位数: -0.01  ✅

奖励分布:
  < -5:   0.0%  ✅ (之前是50%+)
  -5~0:  89.0%  ✅
  0~5:   11.0%  ✅
  > 10:   0.0%  ✅
```

**结论**: 奖励函数已经恢复正常!

---

## 🔧 所有修复项

### 1. 奖励函数 (`train/pool_rl_env.py`)

✅ 移除角度惩罚,改为只奖励不惩罚
✅ 增加进球奖励: 20 → 50
✅ 放宽距离clip: 0.2 → 0.5
✅ 添加时间惩罚: 0 → -0.01
✅ 增加站桩惩罚: -0.5 → -1.0
✅ 只在未命中时才鼓励靠近

### 2. 训练配置 (`train/config.py`)

✅ 并行环境数: 1 → 4
✅ 更新频率: 512 → 2048
✅ 熵系数: 0.01 → 0.02

---

## 🚀 现在可以开始训练

```bash
python -m train.train_phase1
```

### 预期训练曲线

**前50个迭代** (0-100k步):
- 平均奖励: -2 → 0
- 进球率: 0%
- 命中率: 0.1% → 5%

**100-200个迭代** (100k-400k步):
- 平均奖励: 0 → +2
- 进球率: 0% → 2%  (出现第一次进球!)
- 命中率: 5% → 15%

**200-400个迭代** (400k-800k步):
- 平均奖励: +2 → +5
- 进球率: 2% → 10%
- 命中率: 15% → 30%

**关键指标**: 如果400个迭代后进球率达到5-10%,就说明训练成功!

---

## ⚠️ 如果还是学不会

如果训练400个迭代后进球率还是0%,可能的原因:

### 1. 任务本身太难

**诊断**: 命中率 < 1%

**解决**:
- 减小球桌尺寸,让球更容易碰到
- 增加初始距离范围的限制
- 或者先训练"击中目标球"而不是"将球打进洞"

### 2. 网络容量不足

**诊断**: loss下降很慢,value估计不准

**解决**:
```python
NETWORK_CONFIG = {
    'hidden_dim': 512 → 1024,  # 增加网络容量
    'num_hidden_layers': 3 → 4,
}
```

### 3. 学习率太高或太低

**诊断**: loss震荡或不下降

**解决**:
```python
PPO_CONFIG = {
    'lr_actor': 3e-4 → 1e-4,  # 降低学习率
}
```

### 4. 需要课程学习

从更简单的任务开始:
1. Phase 0.5: 只训练击中目标球 (不要求进洞)
2. Phase 1: 训练将球打进洞
3. Phase 2: 对抗BasicAgent

---

## 📝 关键经验教训

1. **不要过度惩罚**: 强化学习要奖励好行为,不要惩罚所有非完美行为
2. **奖励要平衡**: 正反馈 ≥ 负反馈
3. **稀疏为主**: 进球(+50) > 距离改善(+2)
4. **先验证奖励**: 用随机动作测试奖励分布
5. **从简单开始**: 两球模式 → 完整游戏

---

## 🧪 持续监控

训练时观察这些指标:

```bash
# 正常训练应该看到:
[Rollout] 迭代 0001 | 回合 001/256 | 本回合奖励 -0.50  # 初期负奖励正常
[Rollout] 迭代 0050 | 回合 001/256 | 本回合奖励 +0.20  # 逐步上升
[Rollout] 迭代 0100 | 回合 001/256 | 本回合奖励 +2.50  # 开始正奖励
[Rollout] 迭代 0150 | 回合 001/256 | 本回合奖励 +50.0  # 第一次进球!
```

如果200个迭代后还是负奖励,立即停止并重新检查奖励函数!

---

## ✅ 修复完成

所有代码已修改并测试通过,可以开始训练了!

**下一步**:
```bash
python -m train.train_phase1
```

祝训练顺利! 🎉
